{% extends 'base.html.twig' %}

{% block title %}New Publicacion{% endblock %}

{% block body %}
    {# <h1>Create new Publicacion</h1>

    {{ include('publicacion/_form.html.twig') }}

    <a href="{{ path('publicacion_index') }}">back to list</a> #}

    <h1 style="text-align:center" class="font-weight-bold">Crear nueva publicacion</h1>
        <div class="container" style="font-family: Arial, Helvetica, sans-serif; margin-top:2rem; font-weight:bold;">
            <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
                {{ include('publicacion/_form.html.twig') }}
                <br>
                <a href="{{ path('publicacion_index') }}">
                    <button type="button" class="btn btn-info"> Back to list <i class="fas fa-arrow-left"></i> </button> 
                </a>
            </div>
        </div>
{% endblock %}
