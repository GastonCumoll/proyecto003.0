{% extends 'base.html.twig' %}

{% block title %}Suscripcion{% endblock %}

{% block body %}
    <h1 style="text-align:center" class="font-weight-bold">Suscripcion</h1>
    <div class="container" style="font-family: Arial, Helvetica, sans-serif; margin-top:2rem; font-weight:bold;">
        <div class="border border-secondary rounded p-3 shadow mb-5 bg-white ">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ suscripcion.id }}</td>
                    </tr>
                    <tr>
                        <th>Tipo de suscripción</th>
                        <td>{{ suscripcion.tipoPublicacion }}</td>
                    </tr>
                    <tr>
                        <th>Fecha de suscripcion</th>
                        <td>{{ suscripcion.fechaSuscripcion ? suscripcion.fechaSuscripcion|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Usuario</th>
                        <td>{{ suscripcion.getUsuario }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="container">
                <div class="row mt-5">
                    <div class="col-4">
                        <a href="{{ path('suscripcion_index') }}">
                            <button type="button" class="btn btn-info"> Back to list <i class="fas fa-arrow-left"></i> </button>  
                        </a>
                    </div>
                    <div class="col-4 text-center">
                        <a href="{{ path('suscripcion_edit', {'id': suscripcion.id}) }}">
                            <button type="button" class="btn btn-success"> Edit <i class="fas fa-edit"></i></button>
                        </a>
                    </div>
                    <div class="col-4 text-right">
                        {{ include('suscripcion/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>    
        </div>
    </div>
{% endblock %}
